// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Get a free hosted Postgres database in seconds: `npx create-db`
//datasource db {
  //provider = "sqlite"
  //url      = "file:./dev.db"
//}
datasource db {
  provider = "postgresql" // Mude de "sqlite" para "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  name      String?
  password  String
  role      String   @default("USER")
  createdAt DateTime @default(now())
  deletedAt DateTime?
  
  // Relação: Um usuário pode ter muitos registros de dados
  records   DataRecord[]
}

model DataRecord {
  id          String   @id @default(uuid())
  description String
  value       Float
  createdAt   DateTime @default(now())
  deletedAt DateTime?

  // Chave estrangeira para ligar ao usuário
  userId      String
  user        User     @relation(fields: [userId], references: [id])
}

model SystemConfig {
  id            String    @id @default(uuid())
  setupDone     Boolean   @default(false)
  activationKey String    @unique
  createdAt     DateTime  @default(now())
}